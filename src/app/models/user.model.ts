export interface User {
    id: string;
    name: string;
    specilization: string;
    email: string;
    imageUrl: string;
}

export interface Client {
    id: string;
    name: string;
    email: string;
    phone: string;
}